# ShaktiScale AI — Backend dependencies

# Web framework
fastapi
uvicorn[standard]

# Config & validation
pydantic
pydantic-settings

# AI (OpenRouter) — plain HTTP via httpx, no provider SDK needed
httpx

# Testing
pytest
